#include <bits/stdc++.h>
using namespace std;
#define ll long long
int fibo(int n)
{
    if(n<=1)
    return n;
    else
    return fibo(n-1) + fibo(n-2);
}
int main(){
    int t;
    cin>>t;
    while(t--)
    {
    int n;
    cin>>n;
    cout<<fibo(n)<<endl;
    }
}
